# OpenEir realtime voice agent — pinned Pipecat line.
# Extras are chosen so every pluggable engine imports cleanly:
#   silero   → VAD (barge-in detection)
#   openai   → OpenAI-compatible STT/TTS (local whisper / speech servers)
#   deepgram → Deepgram self-hosted STT/TTS
#   whisper  → in-process faster-whisper fallback (no external STT server)
pipecat-ai[silero,openai,deepgram,whisper]~=0.0.66
aiortc>=1.9              # SmallWebRTC transport (pipecat.transports.smallwebrtc) — not covered by the extras above
httpx>=0.27
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7

# Optional extras (comment in as needed):
# pipecat-ai[piper]  # fully offline TTS voices
