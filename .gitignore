# dependencies
node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# env — copy .env.example to .env and adjust
.env
.env.*
!.env.example

# local data — your health record lives here, never commit it
/db/*.db
/db/*.db-journal
/db/*.db-wal
/db/*.db-shm
/db/.appkey

# debug & logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
dev.log
server.log

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts
.vercel

# platform/sandbox tooling — never publish
/skills/
/upload/
/agent-ctx

# internal working notes & local tooling (not part of the published app)
/worklog.md
/.zscripts
/tests
/download
/upload
/Caddyfile
/scripts/verify-*.png
/scripts/rebrand.py
/scripts/migrate-builtin-model.ts

# tesseract language cache & generated scan fixtures
*.traineddata
scripts/test-*.png
tool-results/
.tts-cache/
.z-ai-config
scripts/*.png

# runtime database backups — never publish (health data!)
/db/backups/

# android companion app build artifacts (mobile/android is fully committed)
mobile/node_modules
mobile/android/.gradle
mobile/android/app/build
mobile/android/build
mobile/android/local.properties
mobile/android/capacitor-cordova-android-plugins
mobile/android/app/*.keystore
*.keystore
/openeir-14b84e7f
*-PASSWORD.txt

# agent eval/test scratch databases (hermetic — never real data)
/evals/.scratch.db
/tests/.scratch.db
__pycache__/
.pytest_cache/
